"""Application implementation package for the Cards public-facing service.

The repository-root ``app.py`` remains the deployment entrypoint. Runtime
implementation code lives here so configuration, persistence, HTTP behavior,
domain services, workers, and operational commands have a single discoverable
home.
"""
